## Results

The second stage includes ordinary least squares (OLS) regressions of abnormal returns on the explanatory variables plus $\lambda$ for the proactive recall subsample and the passive recall subsample, separately:

Therefore, significantly negative abnormal stock return is associated with proactive recalls but not with passive recalls. [^9]

$$ \lambda_ {1} = \phi (\gamma^ {\prime} Z) / \Phi (\gamma^ {\prime} Z), \text { and } $$
