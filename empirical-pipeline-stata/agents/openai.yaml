interface:
  display_name: "Empirical Pipeline (Stata)"
  short_description: "Execute a locked design as a full Stata pipeline"
  default_prompt: "Use $empirical-pipeline-stata to execute a locked empirical design in Stata and produce reproducible diagnostics, tables, and figures."
