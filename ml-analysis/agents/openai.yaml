interface:
  display_name: "ML Analysis"
  short_description: "Plan prediction workflows, validation, and leakage checks"
  default_prompt: "Use $ml-analysis to define a prediction target, split, validation plan, and guarded execution handoff."
