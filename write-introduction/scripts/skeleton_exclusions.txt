# Skeleton index — 人工剔除账本
#
# 每行一个 id（可加 `# 原因`）。build_indices.py 每次重跑都会读本文件：
#   1) 命中该 id 的条目不再写入子索引；
#   2) 全部条目列进 `corpus/_skeleton/_unparsed.md` 的「人工剔除」段。
# 这是「脚本可重复再生 + 人工核对剔除」得以并存的关键。
#
# 状态说明：`[已生效]` = 本次生成中确实被剔除；`[回归守卫]` = 解析器已自动避开，保留记录以防回退。

# 引号拼贴：把同一段里的多个引文片段用破折号串成一条，起点在句子中间，不是单条逐字底本。
21-institutionalized-extreme-structural-warrant#1 # [已生效] 引号拼贴（fragment collage），非单条 verbatim 底本

# 备注：id 采用「卡片#文档内序号」，序号随锚点顺序而定；若某卡片新增/删除锚点，
# 请重核本文件中该卡片的 id 后再提交。当前仅保留一条已生效剔除。
