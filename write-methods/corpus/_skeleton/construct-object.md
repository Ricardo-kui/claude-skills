# construct-object — 二级骨架清单（实证对象构建）

> 本目录由脚本重建，手改会被覆盖；重建命令 = `python scripts/build_indices.py`（在 `write-methods/` 目录下）。
> **抽取规则（先定后抽）**：verbatim = 卡片 `**原始句锚点**` / `[原始句锚点]` / `- **原始句锚点**（…）` 内带引号/缩进的完整英文原句（有明确来源论文，逐字保留、含 `…` 不回填）+ `**原文锚定**`/`**原文锚点**`（含 `- **原文锚定**` 子弹式）下 `- "..."` 英文句（id 后缀 .a/.b）；模板 = `**骨架**` / `**模板**` / `**模板/骨架**` / `**结构**` / `[骨架]` 块、代码围栏或紧随裸英文段内带 `[槽位]` 的填槽骨架。
> **citekey** 优先取卡片尾部 `<!-- wb:... -->` 标记，无则回退 `**来源论文**`/`**来源**`/裸 `来源：`/`- **出处**` 原文；EXTEND 子变体回退 `- **原文锚定**` 尾部 `（citekey, …）` 标注；皆无标 `未标注`（不编造）。**适配槽位** 取 `**槽位**`/`[适用槽位]` 字段内 M1–M10（含 M2.5）与 Q1–Q8（去重排序）；无字段或含 `M?` 标 `通用`。
> **锚点** = `corpus/<文件名>#变体-<变体号>`（脚本自定义片段，指向 `### 变体 <N>` 标题；不编号/EXTEND 变体指向其真实 `### 变体：`/`#### 变体：` 标题；`--verify` 断言标题存在）。
> 状态列：`verbatim` = 逐字底本（与源卡片逐字一致，不得改写/拼接/补全）；`模板` = 填槽骨架（不可当逐字底本引用）。`不编号变体`（fang2025 POM，不计入 342）与 `EXTEND子变体`（`####` 层，不计入 342）在 id 与状态列标注。

条目：verbatim 17 条 / 模板 17 条。

## Verbatim 底本

| id | 适配槽位 | citekey | 句子原文（或模板） | 卡片路径#锚点 | 状态 |
|---|---|---|---|---|---|
| `construct-object#1` | M3 | zhao_ding_gaba_orsc | Following prior research, we calculate the reciprocal of the daily rank in the photo video grossing list for each app as a proxy for daily revenue and calculate the monthly revenue proxy by aggregating these reciprocals over a particular month (Kapoor and Agarwal 2017). We define successful apps as those above the 75th percentile in the monthly revenue proxy within the core function, in the time period three months prior to the focal app entry. | `corpus/实证对象构建.md#变体-1` | verbatim |
| `construct-object#6` | M2 | gulati2005-adaptation-vertical | Unlike prior studies that aggregate all transactions relevant to a particular component (e.g., Monteverde and Teece, 1982), our analysis is truly at the transaction level. | `corpus/实证对象构建.md#变体-6` | verbatim |
| `construct-object#13` | M2 | mao_dong_lee_2022_msom | we restrict our data to auto models sold in the U.S. market from January 2012 to August 2015, which yields 27 manufacturers, 49 brands, and 529 car models with 15,361 model-month observations in our data set. | `corpus/实证对象构建.md#变体-13` | verbatim |
| `construct-object#2` | M2 | Cui, Yang & Vertinsky (Strategic Management Journal) | We specifically identified drugs approved under clauses 505(b)(2) and 505(j) of the Federal Food, Drug, and Cosmetic Act (FD&C Act) with the aid of the FDA's Approved Drug Products with Therapeutic Equivalence Evaluations (the "Orange Book"). Clause 505(j) governs generic drug applications, while clause 505(b)(2) governs the modifications of previously approved brand-name drugs. | `corpus/实证对象构建.md#变体-2` | verbatim |
| `construct-object#3` | M4 | du_tsolmon_2024_os | We scale the base counts by adding 0.1 to each count to incorporate cases of zero base unit counts (e.g., a standalone firm with no subsidiaries) while preserving the scale of the base unit counts. To illustrate, when the acquirer and target have the same number of units (e.g., both with five base units), the similarity index equals one: 1 − \|5 − 5\|/(5 + 5) = 1.0. | `corpus/实证对象构建.md#变体-3` | verbatim |
| `construct-object#4` | M3 | pollock_2015_asq | It is an objective as opposed to perceptual measure of reputation in that it is composed of objective measures expected to influence stakeholders' perceptions. Objective reputation measures are often employed when perceptual measures are unavailable, and they can be constructed retrospectively for use in longitudinal analyses. | `corpus/实证对象构建.md#变体-4` | verbatim |
| `construct-object#5` | M3/M4 | pontikes_2012_asq | Building on this, I used the opposite of contrast, fuzziness, as one measure of label ambiguity. When fuzziness is high, it means a large number of affiliated organizations also identify with at least one other label. | `corpus/实证对象构建.md#变体-5` | verbatim |
| `construct-object#9` | M4 | desjardine_2025_information_based_competition_the_case_of_ri | We identified rival firms using the fixed industry classification (FIC) (Hoberg & Phillips, 2010, 2016). ... where firms are legally required to report accurate descriptions of their key products. | `corpus/实证对象构建.md#变体-9` | verbatim |
| `construct-object#10` | M3/M4 | pfarrer_pollock_and_rindova_2010 | There is no one standard method for operationalizing material earnings surprises. ... we followed past research identifying material earnings surprises on the basis of percentiles ... and defined material earnings surprises as the top and bottom quartiles of positive and negative surprises in a given industry in a given year. ... we used the initial universe of 2,417 firms in Compustat ... and not simply the 291 firms in the matched sample. | `corpus/实证对象构建.md#变体-10` | verbatim |
| `construct-object#14` | M3/M4 | mao_dong_lee_2022_msom | Based on the monthly recall report, we identify three key time stamps—a defect's notice time, its identification time, and its recall time—and calculate their corresponding product cycle status, which is the ratio of the current product age when the event occurred divided by the total sales duration | `corpus/实证对象构建.md#变体-14` | verbatim |
| `construct-object#7` | M2 | gulati2005-adaptation-vertical | We ensured that each survey respondent was the expert for a given component by verifying his or her expert status with the controller's office in each company. | `corpus/实证对象构建.md#变体-7` | verbatim |
| `construct-object#8` | M3/M4 | gulati2005-adaptation-vertical | The response to this question was corroborated with two other survey items in which respondents indicated the percentage of the given component sourced through each of the three types of arrangements, and the expected duration of each relationship. | `corpus/实证对象构建.md#变体-8` | verbatim |
| `construct-object#11` | M1 | mao_dong_lee_2022_msom | Next, we use data from the U.S. automobile industry to study the applicability of our proposed recall-timing decision tree on those high-harm defects that eventually lead to recalls. | `corpus/实证对象构建.md#变体-11` | verbatim |
| `construct-object#12` | M3 | mao_dong_lee_2022_msom | Following Eilert et al. (2017), we use the principal component analysis (PCA) to give a univariate score to each recall on the number of (1) vehicles affected, (2) crashes, (3) fires, (4) injuries, and (5) deaths in a single recall campaign. | `corpus/实证对象构建.md#变体-12` | verbatim |
| `construct-object#15` | M2 | mao_dong_lee_2022_msom | We note that our data only cover product defects that eventually lead to recalls and only include the NHTSA database records recalling firms' actions when high-harm defects are identified. | `corpus/实证对象构建.md#变体-15` | verbatim |
| `construct-object#16` | M7 | mao_dong_lee_2022_msom | We then use the nonlinear least-squares estimator to jointly estimate the three equations in (3) by utilizing Equations (4) and (5) at different time stamps, and we assume that the error terms for each equation are correlated. | `corpus/实证对象构建.md#变体-16` | verbatim |
| `construct-object#17` | M7/M10 | mao_dong_lee_2022_msom | the internal influence of word of mouth significantly outweighs the external influence of mass media for auto purchases in terms of the magnitude of coefficients, which is consistent with the literature (e.g., Bass 1969), and also justifies the model assumption that λ > γ | `corpus/实证对象构建.md#变体-17` | verbatim |

## 填槽模板

| id | 适配槽位 | citekey | 句子原文（或模板） | 卡片路径#锚点 | 状态 |
|---|---|---|---|---|---|
| `construct-object#T1` | M3 | zhao_ding_gaba_orsc | Our [second] dependent variable is [DV_name], which captures the extent to which a [unit] [differentiates from / imitates] existing [successful units] sharing the same [core construct]. [Peripheral constructs] are defined as [meaningful noncore constructs] in a [unit]. By design of the [model], all [constructs] have a [positive weight], but not every [noncore construct] is equally important. As [weights add up to one], and there are [K] [constructs] in total, we consider [weights more than 1/K] as meaningful and count a particular [construct] as [peripheral] when its weight is [less than the core construct weight and more than 1/K]. To identify [successful units], we utilize the [performance metric] in the [category] to measure performance. In the [platform], data on [revenues] are not publicly available, but [platform operator] discloses [ranking lists] by [metric_1] and by [metric_2], within and across [categories]. [Units] that rank higher on the lists offer clear evidence of [performance superiority] ([citation]). Following prior research, we calculate the [reciprocal of the daily rank] in the [grossing list] for each [unit] as a proxy for [daily revenue] and calculate the [monthly revenue proxy] by aggregating these [reciprocals] over a particular [month] ([citation]). We define [successful units] as those above the [75th percentile] in the [monthly revenue proxy] within the [core construct], in the [time period] [N months] prior to the [focal unit entry]. [DV_name] is calculated as the [average pairwise Jaccard similarity] between the [focal unit] and all [successful units]. For both the [focal unit] and a [successful unit], we find the [sets of peripheral constructs] they incorporate in support of the [core construct]. [Jaccard similarity] is calculated by the [intersection] of the two [sets] divided by the [union] of the two [sets] (J(A,B) = (A∩B)/(A∪B)). The [intersection] reflects the [overlap] between the [units], and the [union] captures the [total space] incorporated by both [units]. | `corpus/实证对象构建.md#变体-1` | 模板 |
| `construct-object#T6` | M2 | gulati2005-adaptation-vertical | [层级宣告] Unlike prior studies that aggregate [higher-level unit] ([benchmark cite]), our analysis is truly at the [finer unit] level. [数据结构句] Each [respondent] provided data on the [focal unit] he or she was in charge of sourcing, as well as data on the [K largest subunits] for that [unit] ([K−1] if only [K−1] existed). | `corpus/实证对象构建.md#变体-6` | 模板 |
| `construct-object#T13` | M2 | mao_dong_lee_2022_msom | To match the [event] data, we restrict our data to [population] sold in [market] from [start] to [end], which yields [count_level_1], [count_level_2], and [count_level_3] with [N] [unit-period] observations in our data set. Of the [count_level_3] [units] that we include in our final sample, [n] [units] [experienced event]. Our unit of analysis is thus the set of [n] (first) [events] associated with these [n] [units]. | `corpus/实证对象构建.md#变体-13` | 模板 |
| `construct-object#T2` | M2 | Cui, Yang & Vertinsky (Strategic Management Journal) | To measure [product competition], we began by selecting all [regulator]-approved [products] from [directory]. We specifically identified [products] approved under [regulatory clauses] with the aid of [reference source]. [Clause A] governs [product type A], while [clause B] governs [product type B]. Under these pathways, the timeline for most new [product] launches is reduced to approximately [time]. We then collected information on each [product]'s [attributes] from [N] specialized databases: [source 1], [source 2], [source 3], and [source 4] ([citation]). A researcher in [discipline] who was not part of our team used this information to help classify all listed [products] into competing groups. For example, [example A] and [example B] are considered direct competitors in [market], since they use the same [mechanism]. We then linked the competing [products] to their producers and market introduction dates with the aid of [source]. | `corpus/实证对象构建.md#变体-2` | 模板 |
| `construct-object#T3` | M4 | du_tsolmon_2024_os | We measure [similarity construct] at the [unit] level and construct a continuous variable for each [pair] of the degree to which the [unit A] and [unit B] have similar [attribute]. We measure the degree of [attribute] as [countable operationalization] ([citation]). These [units] represent [theoretical meaning], and therefore can be compared across [units]. We calculate the [similarity index] using the formula: 1 − \|[A_count] − [B_count]\| / ([A_count] + [B_count]), where we scale the counts by adding [small constant] to each to incorporate cases of zero counts while preserving the scale. The normalized index ranges from zero to one, where zero indicates completely dissimilar [attribute], and one indicates identical [attribute]. [Numerical example 1: identical → index = 1.0]. [Numerical example 2: moderate → index = 0.6]. [Numerical example 3: extreme → index → 0]. The sample mean of the index is [value]. We also construct a binary measure based on [median/median-split criterion] as an alternative measure in robustness checks. | `corpus/实证对象构建.md#变体-3` | 模板 |
| `construct-object#T4` | M3 | pollock_2015_asq | "Our [construct] measure is a modified version of the [prior index name] index ([citation]). The [index] is a multi-item, time-varying index that reflects [actor]'s ability to [theoretical dimension 1] and [theoretical dimension 2], the [N] performance dimensions important to [stakeholders] ([citation]). It is an **objective as opposed to perceptual** measure of [construct] in that it is composed of objective measures expected to influence stakeholders' perceptions. Objective [construct] measures are often employed when perceptual measures are unavailable, and they can be constructed retrospectively for use in longitudinal analyses. Our index included [N] formative indicators of [actor] [construct]: (1) [indicator 1], (2) [indicator 2], (3) [indicator 3], (4) [indicator 4], and (5) [indicator 5]. [The prior index] also included [variable], which we excluded from our index because we used it as a [moderator] in our theory and analyses. [These indicators] were also standardized and aggregated using a [N]-year moving average. As in prior work, we needed to make the values comparable across time. [Market dynamics] can make cross-year comparisons difficult; for example, what was considered a large [indicator value] was very different in [year 1] and [year 2]. Thus we rescaled the index so that within-year differences are maintained while cross-year variance in values due to [market conditions] is eliminated, and we converted our measure to a [100]-point scale that is comparable across years. As the standardized index can take on negative values, for each year we added a constant equal to .01 plus the positive value of the lowest score that year to all scores (making all values positive) and divided each score by the largest value that year. We then multiplied the resulting value by 100. This rescaling allows for cross-year comparisons while maintaining the relative ratings among [actors] within each year. Like [other construct], this final measure was standardized so that the relative effects of [the two constructs] could be compared." | `corpus/实证对象构建.md#变体-4` | 模板 |
| `construct-object#T5` | M3/M4 | pontikes_2012_asq | "To test [hypotheses about construct X—e.g., label ambiguity], I needed to create measures of [X]. I drew on research showing that [when members of a label primarily identify elsewhere, the focal label is less likely to develop a shared social meaning] ([citations]). Previous research built on the concept of a label's [contrast]—how clearly a label is defined relative to others ([citation]). I used the opposite of contrast, **[fuzziness]**, as one measure of [X]: [fuzz = 1 − contrast]; when [fuzziness] is high, [many affiliated actors also identify with at least one other label]. I also created an alternate measure, **[leniency]**, which captures [the extent to which a label constrains affiliated actors]. [A label can be fuzzy if all its members identify with the SAME alternate label—high overlap but still constraining]. [Leniency] builds on [fuzziness] but also takes into account whether a label overlaps with [many DIFFERENT labels]: [leniency = fuzz × ln(NO_distinct_other_labels)]. I conceived of labels as **fuzzy sets**, so that an actor can be partially associated with a label. I calculated an actor's **grade of membership** in a label as [the number of times it mentioned the label / total mentions of any label] in [year], taking values between 0 and 1. [Contrast = fuzzy density / support]; [fuzz = 1 − contrast]. To measure an actor i's labels' [fuzziness/leniency], I computed a weighted average: [Σ over labels μ_i(l) × fuzz_l], weighting each label's [fuzziness] by the actor's grade of membership in it." | `corpus/实证对象构建.md#变体-5` | 模板 |
| `construct-object#T9` | M4 | desjardine_2025_information_based_competition_the_case_of_ri | We identified [rival firms] using the [text-based industry classification] ([citation]). [Classification authors] identified pairs of [competitors] using a [firm-by-firm similarity score] derived from analyzing the text of [units'] [product/activity descriptions] in [regulated disclosure source], where [units] are legally required to report accurate descriptions of their key [products]. Based on the [similarity score] in [base year], they used a clustering algorithm to generate [a menu of K granularity levels] of industry classifications that maximizes the overall [similarity score] within each [industry]. Holding the set of [industries] fixed over time, [the authors] have updated the constituents of each [industry] annually based on [units'] most recent [descriptions]. | `corpus/实证对象构建.md#变体-9` | 模板 |
| `construct-object#T10` | M3/M4 | pfarrer_pollock_and_rindova_2010 | There is no one standard method for operationalizing [material X]. Scholars studying [X] have routinely used different [dollar amounts / percentages / percentile cutoffs] to delineate them ([cites]). To avoid [distortion_A: scaling by a size-sensitive denominator] ([cite]) or employing arbitrary cutoff values having different meanings in different [periods] and [segments] ([cite]), we followed past research ([cites]) and defined [material X] as the top and bottom quartiles of [positive/negative X] in a given [industry] in a given [year]. To calculate the top and bottom quartiles, we used the initial universe of [N] [units] in [database] — and not simply the [n] [units] in the matched sample. | `corpus/实证对象构建.md#变体-10` | 模板 |
| `construct-object#T14` | M3/M4 | mao_dong_lee_2022_msom | Based on the [process records], we identify [k] key time stamps—a [object]'s [event_1] time, its [event_2] time, and its [event_3] time—and calculate their corresponding [process-clock status], which is the ratio of the current [unit] age when the [event] occurred divided by the total [duration] (as reported in [database]). | `corpus/实证对象构建.md#变体-14` | 模板 |
| `construct-object#T7` | M2 | gulati2005-adaptation-vertical | [组织提名] For each [unit], [sponsoring body] provided us with the names of individuals with oversight over [the unit]. We mailed our survey to these individuals. [独立核验] We ensured that each respondent was the [expert role] for a given [unit] by verifying [his/her] expert status with [independent verification office inside each organization]. [自除名通道] Our cover letter indicated that [the respondent] had been identified as an [expert role] on the acquisition of a given [unit] and asked for the return of the uncompleted survey and a nomination of an [alternative expert] if this were not the case. | `corpus/实证对象构建.md#变体-7` | 模板 |
| `construct-object#T8` | M3/M4 | gulati2005-adaptation-vertical | [问卷内定义] Respondents identified each of [K] [units] as falling into one of [K] categories defined in the questionnaire: (a) [category 1, with defining features embedded]; (b) [category 2]; and (c) [category 3]. [行为性旁证] The response to this question was corroborated with [N] other survey items in which respondents indicated [quantitative attribute consistent with category membership] and [temporal attribute of the relationship]. [分布宣告] Of the [N] [units] in our sample, [n1] were organized as [category 1], [n2] as [category 2], and [n3] as [category 3]. | `corpus/实证对象构建.md#变体-8` | 模板 |
| `construct-object#T11` | M1 | mao_dong_lee_2022_msom | Next, we use data from [industry/context] to study the applicability of our proposed [theoretical object: decision rule / policy / classification scheme] on those [cases] that [model scope condition]. To do so, we collect our data from [database_1] and [database_2]. The [database_1] contains both [outcome-side variables, e.g., recalling brand and model, number of affected units, event dates] and [process-side information, e.g., number of complaints/crashes/fires/injuries per recorded event]. | `corpus/实证对象构建.md#变体-11` | 模板 |
| `construct-object#T12` | M3 | mao_dong_lee_2022_msom | As not all [events] exert a material effect on [firms/consumers], we only study [event class], because [audience] have become largely insensitive to [events] due to their high frequency and media exposure. Following [precedent cite], we use [data-reduction method] to give a univariate score to each [event] on the number of (1) [dimension_1], (2) [dimension_2], (3) [dimension_3], (4) [dimension_4], and (5) [dimension_5] in a single [event]. We only focus on [event class] with [score] above the [percentile] percentile cutoff. Specifically, we classify [N_total] [events] from [database] as [event class], [n_A] of which were [initiator_A] and [n_B] of which were [initiator_B]. | `corpus/实证对象构建.md#变体-12` | 模板 |
| `construct-object#T15` | M2 | mao_dong_lee_2022_msom | We note that our data only cover [subset of the theoretical domain, e.g., cases that eventually reach the outcome] and only include [source-side limitation, e.g., the database records the focal actors' actions when a condition holds]. | `corpus/实证对象构建.md#变体-15` | 模板 |
| `construct-object#T16` | M7 | mao_dong_lee_2022_msom | We follow [estimation precedent] to estimate the [structural model] in Equation ([n]). Specifically, we discretize the continuous [process pattern] and denote [symbol] as the [outcome] of [unit] in [period], and it is estimated by [estimating equation]. By solving the [first-order condition/differential equation] in Equation ([n]), we know that [auxiliary function] follows the form [closed-form solution]. To capture the [shape] pattern of [time-varying functions], we assume a linear form for these two, in which [slope parameters] measure the (constant) marginal time effect. In sum, the coefficients to be estimated include [parameter inventory]. We then use the [estimator] to jointly estimate the [k] equations in [model equation] by utilizing Equations ([a]) and ([b]) at different [time stamps], and we assume that the error terms for each equation are correlated. | `corpus/实证对象构建.md#变体-16` | 模板 |
| `construct-object#T17` | M7/M10 | mao_dong_lee_2022_msom | Specifically, both [influence_A] and [influence_B] are significantly positive. Moreover, the [influence_A] of [channel_1] significantly outweighs the [influence_B] of [channel_2] for [outcome] in terms of the magnitude of coefficients, which is consistent with the literature ([cite]), and also justifies the model assumption that [structural assumption]. Note that [slope_A] is significantly negative, indicating that [substantive interpretation_A]; [slope_B] is also significantly negative, which suggests that [substantive interpretation_B]. Moreover, the absolute value of the coefficient of [slope_A] is far larger than that of [slope_B], which suggests that [comparative substantive claim]. Finally, the [scale parameter] is estimated to be [value] based on our [model name]. | `corpus/实证对象构建.md#变体-17` | 模板 |
