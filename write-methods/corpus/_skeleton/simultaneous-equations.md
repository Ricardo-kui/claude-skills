# simultaneous-equations — 二级骨架清单（同时方程）

> 本目录由脚本重建，手改会被覆盖；重建命令 = `python scripts/build_indices.py`（路径基准：以本 skill 目录（SKILL.md 所在目录）为基准）。
> **抽取规则（先定后抽）**：verbatim = 卡片 `**原始句锚点**` / `[原始句锚点]` / `- **原始句锚点**（…）` 内带引号/缩进的完整英文原句（有明确来源论文，逐字保留、含 `…` 不回填）+ `**原文锚定**`/`**原文锚点**`（含 `- **原文锚定**` 子弹式）下 `- "..."` 英文句（id 后缀 .a/.b）；模板 = `**骨架**` / `**模板**` / `**模板/骨架**` / `**结构**` / `[骨架]` 块、代码围栏或紧随裸英文段内带 `[槽位]` 的填槽骨架。
> **citekey** 优先取卡片尾部 `<!-- wb:... -->` 标记，无则回退 `**来源论文**`/`**来源**`/裸 `来源：`/`- **出处**` 原文；EXTEND 子变体回退 `- **原文锚定**` 尾部 `（citekey, …）` 标注；皆无标 `未标注`（不编造）。**适配槽位** 取 `**槽位**`/`[适用槽位]` 字段内 M1–M10（含 M2.5）与 Q1–Q8（去重排序）；无字段或含 `M?` 标 `通用`。
> **锚点** = `corpus/<文件名>#变体-<变体号>`（脚本自定义片段，指向 `### 变体 <N>` 标题；不编号/EXTEND 变体指向其真实 `### 变体：`/`#### 变体：` 标题；`--verify` 断言标题存在）。
> 状态列：`verbatim` = 逐字底本（与源卡片逐字一致，不得改写/拼接/补全）；`模板` = 填槽骨架（不可当逐字底本引用）。`不编号变体`（fang2025 POM，不计入 342）与 `EXTEND子变体`（`####` 层，不计入 342）在 id 与状态列标注。

条目：verbatim 5 条 / 模板 5 条。

## Verbatim 底本

| id | 适配槽位 | citekey | 句子原文（或模板） | 卡片路径#锚点 | 状态 |
|---|---|---|---|---|---|
| `simultaneous-equations#1` | M7 | Pollock et al. 2015 (ASQ) | Second, this model incorporates the simultaneously determined nature of status and reputation by including the current measures of each variable as an independent variable in the other variable's estimation equation. Thus this model implicitly presumes that two reciprocal causal forces are at work simultaneously (Wooldridge, 2010). | `corpus/同时方程.md#变体-1` | verbatim |
| `simultaneous-equations#2` | M8 | Pollock et al. 2015 (ASQ) | Given that these two models are non-nested and thus estimated separately, this covariance is not automatically generated from the estimation of individual models. To obtain this covariance, we followed the procedure recommended by Weesie (1999) and used the stack command in STATA to combine the two non-nested datasets into a single "stacked" dataset in which each set of the two non-nested but jointly determined observations (i.e., one from the status model and the other from the reputation model) was given the same identifier. | `corpus/同时方程.md#变体-2` | verbatim |
| `simultaneous-equations#3` | M7/M8 | Malshe & Agarwal 2015 (JM) | Thus, anticipated fund needs can affect decisions about leverage. To account for these potential effects, we include lagged advertising, R&D, and customer satisfaction as predictor variables. | `corpus/同时方程.md#变体-3` | verbatim |
| `simultaneous-equations#4` | M7/M8 | Malshe & Agarwal 2015 (JM) | The econometrics literature has advocated for the use of seemingly unrelated regression (SUR) estimation for estimating a system of equations because it provides consistent and efficient estimates by modeling the correlated error terms across equations (Wooldridge 2010). Following the well-established literature stream in marketing and econometrics (Hennig-Thurau, Henning, and Sattler 2007; Stahl et al. 2012; Ter Braak, Dekimpe, and Geyskens 2013; Wooldridge 2010), we used the Durbin–Wu–Hausman test to check for endogeneity. | `corpus/同时方程.md#变体-4` | verbatim |
| `simultaneous-equations#5` | M8 | kalaignanam_2013_jm | Thus, the errors of Model 2b and Model 3b are independent, and three-stage least squares estimation will not yield more efficient estimates than two-stage least squares estimates. | `corpus/同时方程.md#变体-5` | verbatim |

## 填槽模板

| id | 适配槽位 | citekey | 句子原文（或模板） | 卡片路径#锚点 | 状态 |
|---|---|---|---|---|---|
| `simultaneous-equations#T1` | M7 | Pollock et al. 2015 (ASQ) | We modeled the [coevolutionary] processes of [construct A] and [construct B] as a system of two interdependent structural equations: [A_it = ρ1·A_{i,t−1} + β1·B_it + γ1·X_it + μ1i + ε1it]; [B_it = ρ2·B_{i,t−1} + β2·A_it + γ2·X_it + μ2i + ε2it]. This model incorporates three essential features. First, it conceptualizes the evolutionary processes of [A] and [B] accumulation as dynamic and path dependent ([citations]), causing serial correlation of the error terms unless controlled for. Using the lagged dependent variable as an explanatory variable takes path dependence into consideration and addresses serial correlation stemming from persistence. The degrees of path dependence or persistence are reflected in ρ, the coefficients of the lagged values. Second, this model incorporates the simultaneously determined nature of [A] and [B] by including the current measures of each variable as an independent variable in the other variable's estimation equation; thus this model implicitly presumes that two reciprocal causal forces are at work simultaneously ([citation]). Third, this model accounts for unobserved heterogeneity using fixed effects, which is important because we used panel data. | `corpus/同时方程.md#变体-1` | 模板 |
| `simultaneous-equations#T2` | M8 | Pollock et al. 2015 (ASQ) | To test [whether the effect of A on B differs from the effect of B on A] we compared the [A] and [B] coefficients across two models—one in which [A] predicted [B] and one in which [B] predicted [A]—and assessed whether the effects of [A on B] were different than the effects of [B on A] across [conditions]. We employed the Wald χ²-test; the null hypothesis assumed the non-nested models' parameter estimates are the same. [Test statistic: (β1 − β2)² / var(β1 − β2), distributed χ² with one degree of freedom.] This statistic requires not only each parameter estimate's variance but also the covariance between them. Given that these two models are non-nested and thus estimated separately, this covariance is not automatically generated from the estimation of individual models. To obtain this covariance, we followed the procedure recommended by [Weesie (1999)] and used the *stack* command in STATA to combine the two non-nested datasets into a single 'stacked' dataset in which each set of the two non-nested but jointly determined observations (i.e., one from the [A] model and the other from the [B] model) was given the same identifier. This allowed us to cluster the two non-nested models and generate covariances using the [estimator] command with the *vce(cluster)* option specified. The parameter estimates generated by the stacked model were exactly the same as those generated by the individual models. | `corpus/同时方程.md#变体-2` | 模板 |
| `simultaneous-equations#T3` | M7/M8 | Malshe & Agarwal 2015 (JM) | We added Equation [N] to account for the notion that the anticipated need for funds can influence firms' [policy/endogenous variable] decisions. [Actors] can fund their increased [downstream activity] (e.g., [examples]) from their internal sources, debt, or equity. For example, [Author] ([year]) show that [firms use internal cash flows to finance the downstream activity]. A stream of [source] literature has argued that [actors] with more [downstream assets] will tend to [choose different policy levels] because [theoretical reason] ([citations]). Along the same lines, [related] literature has argued that [actors] in need of [ongoing funding] will want [a different policy level], which provides greater [slack/flexibility] ([citation]). Thus, anticipated [downstream] needs can affect decisions about [the policy variable]. To account for these potential effects, we include lagged [downstream variable 1], [downstream variable 2], and [downstream variable 3] as predictor variables [in the policy-variable equation]. | `corpus/同时方程.md#变体-3` | 模板 |
| `simultaneous-equations#T4` | M7/M8 | Malshe & Agarwal 2015 (JM) | The econometrics literature has advocated for the use of [seemingly unrelated regression (SUR)] estimation for estimating a system of equations because it provides consistent and efficient estimates by modeling the correlated error terms across equations ([citation]). However, in the presence of endogeneity, it may be necessary to use instrumental variables. Prior literature has suggested using [lags of dependent variables] as instruments (e.g., [citation]). We used a combination of [third and fourth lags] of endogenous variables as instruments in a [three-stage least squares] estimation and performed the [Hansen–Sargan test] of overidentifying restrictions to determine the validity of the instruments. The test statistic was statistically nonsignificant (χ²([df]) = [value], p > .10), suggesting that the instruments are valid. Next, we examined whether the [SUR] estimates differed significantly from the [3SLS] estimates. Following the well-established literature stream ([citations]), we used the [Durbin–Wu–Hausman] test to check for endogeneity. The nonsignificant test statistic (χ²([df]) = [value], p > .10) suggests that the [SUR] estimates are preferable to the estimates of instrumental regressions and that endogeneity is not a concern. | `corpus/同时方程.md#变体-4` | 模板 |
| `simultaneous-equations#T5` | M8 | kalaignanam_2013_jm | Fourth, the errors in [equation A] and [equation B] may be correlated. If so, seemingly unrelated regressions might increase the efficiency of the estimates. We performed the [Breusch-Pagan] test to check for the dependence of the errors. The chi-square statistic with one degree of freedom is [value] (p > [threshold]). In addition, the bivariate correlation between the errors of [A] and [B] is [r]. Thus, the errors of [A] and [B] are independent, and [3SLS] estimation will not yield more efficient estimates than [2SLS] estimates. | `corpus/同时方程.md#变体-5` | 模板 |
