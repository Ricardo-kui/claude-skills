interface:
  display_name: "Write Results"
  short_description: "Report Results as evidence-backed story resolution"
  default_prompt: "Use $write-results to report the evidence and resolve each declared storyline without overclaiming."
policy:
  allow_implicit_invocation: true
